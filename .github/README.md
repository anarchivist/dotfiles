## anarchivist/dotfiles

my dotfiles managed by [yadm](https://yadm.io). prerequisites are just yadm, so install it first.

templates:

* .bash\_profile
* .gitcomfig

if templates are not generating things, try running `yadm alt`. 
